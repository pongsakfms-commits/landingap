# � คู่มือการใช้งาน LearnPro Academy CMS

## สารบัญ
1. [ภาพรวมระบบ](#1-ภาพรวมระบบ)
2. [การติดตั้ง](#2-การติดตั้ง)
3. [Admin Panel](#3-admin-panel)
4. [การจัดการเนื้อหา](#4-การจัดการเนื้อหา)
5. [การจัดการธีม](#5-การจัดการธีม)
6. [การตั้งค่า SEO](#6-การตั้งค่า-seo)
7. [การตั้งค่าแผนที่](#7-การตั้งค่าแผนที่)
8. [การย้าย Domain](#8-การย้าย-domain)
9. [ความปลอดภัย](#9-ความปลอดภัย)
10. [การแก้ปัญหา](#10-การแก้ปัญหา)

---

## 1. ภาพรวมระบบ

LearnPro Academy CMS เป็นระบบจัดการ Landing Page สำหรับธุรกิจคอร์สเรียนออนไลน์ ประกอบด้วย:

### Frontend (หน้าเว็บสาธารณะ)
- **Hero Section** - ส่วนแนะนำหลัก พร้อมหัวข้อ, รูปภาพ, สถิติ
- **Curriculum Section** - แสดงหลักสูตรทั้งหมด
- **Pricing Section** - ตารางราคาแพ็กเกจ
- **Contact Section** - ข้อมูลติดต่อพร้อมแผนที่
- **Footer** - Social Links และ Copyright

### Backend (Admin Panel)
- **Dashboard** - ภาพรวมสถิติ
- **Settings** - ตั้งค่าเว็บไซต์ (8 กลุ่ม)
- **Courses** - จัดการคอร์สเรียน
- **Pricing** - จัดการแพ็กเกจราคา
- **Themes** - ปรับแต่งธีมสี
- **SEO** - ตั้งค่า SEO และ Schema.org

---

## 2. การติดตั้ง

### ความต้องการของระบบ
- PHP 7.4 ขึ้นไป
- MySQL 5.7+ หรือ MariaDB 10.3+
- Apache หรือ Nginx (เปิด mod_rewrite)
- GD Library (สำหรับ CAPTCHA)

### ขั้นตอนการติดตั้ง

#### ขั้นตอนที่ 1: อัพโหลดไฟล์
อัพโหลดไฟล์ทั้งหมดไปยัง Web Server ของคุณ

#### ขั้นตอนที่ 2: สร้าง Database
```sql
CREATE DATABASE landingap_db 
CHARACTER SET utf8mb4 
COLLATE utf8mb4_unicode_ci;
```

#### ขั้นตอนที่ 3: Import SQL
```bash
mysql -u root -p --default-character-set=utf8mb4 landingap_db < sql/database.sql
mysql -u root -p --default-character-set=utf8mb4 landingap_db < sql/seo_themes.sql
```

#### ขั้นตอนที่ 4: ตั้งค่า Config

**config/database.php:**
```php
define('DB_HOST', 'localhost');
define('DB_NAME', 'landingap_db');
define('DB_USER', 'your_username');
define('DB_PASS', 'your_password');
```

**config/config.php:**
```php
define('SITE_URL', 'https://yourdomain.com');
```

#### ขั้นตอนที่ 5: ตั้งค่า Permissions
```bash
chmod 755 uploads/
chmod 755 logs/
```

#### ขั้นตอนที่ 6: เข้าใช้งาน
- Frontend: `https://yourdomain.com/index.php`
- Admin: `https://yourdomain.com/admin/login.php`
- **Username:** `admin`
- **Password:** `password`

⚠️ **สำคัญ:** เปลี่ยนรหัสผ่านทันทีหลังติดตั้ง!

---

## 3. Admin Panel

### การเข้าสู่ระบบ
1. ไปที่ `https://yourdomain.com/admin/login.php`
2. กรอก Username และ Password
3. กรอกรหัส CAPTCHA (ตัวอักษร 5 ตัว)
4. คลิก "เข้าสู่ระบบ"

### Dashboard
หลังเข้าสู่ระบบจะเห็น Dashboard แสดง:
- จำนวนคอร์สทั้งหมด
- จำนวนแพ็กเกจราคา
- จำนวนการตั้งค่า
- Quick Actions สำหรับเข้าถึงส่วนต่างๆ

### เมนูหลัก
- **📊 Dashboard** - หน้าหลัก
- **⚙️ ตั้งค่าเว็บไซต์** - จัดการ Settings ทั้ง 8 กลุ่ม
- **📚 จัดการคอร์ส** - เพิ่ม/แก้ไข/ลบคอร์ส
- **💰 จัดการแพ็กเกจ** - เพิ่ม/แก้ไข/ลบแพ็กเกจราคา
- **🎨 จัดการธีม** - เปลี่ยนสีธีม
- **🔍 ตั้งค่า SEO** - Meta tags และ Schema.org
- **🔒 ออกจากระบบ**

---

## 4. การจัดการเนื้อหา

### 4.1 ตั้งค่าทั่วไป (General)
ไปที่ **ตั้งค่าเว็บไซต์ > ⚙️ ทั่วไป**

| ฟิลด์ | คำอธิบาย |
|-------|----------|
| ชื่อเว็บไซต์ | ชื่อที่แสดงใน Logo และ Title |
| คำอธิบายเว็บไซต์ | ข้อความอธิบายสั้นๆ |
| โลโก้ | อัพโหลดรูปหรือใส่ URL |
| Favicon | ไอคอนแสดงบน Tab เบราว์เซอร์ |

### 4.2 Hero Section
ไปที่ **ตั้งค่าเว็บไซต์ > 🦸 Hero Section**

| ฟิลด์ | คำอธิบาย |
|-------|----------|
| หัวข้อหลัก บรรทัด 1 | ข้อความบรรทัดแรก |
| หัวข้อหลัก บรรทัด 2 | ข้อความบรรทัดที่ 2 (Gradient) |
| คำอธิบาย | ข้อความอธิบายใต้หัวข้อ |
| รูปผู้สอน | รูปภาพหลัก |
| ปุ่มหลัก/รอง | ข้อความบนปุ่ม |
| สถิติ 1-3 | ตัวเลขและป้าย |

### 4.3 จัดการคอร์ส
ไปที่ **📚 จัดการคอร์ส**

#### เพิ่มคอร์สใหม่:
1. คลิก "➕ เพิ่มคอร์สใหม่"
2. กรอกข้อมูล:
   - หมวดหมู่ (เช่น Web Development)
   - ไอคอน (Emoji เช่น 💻)
   - ชื่อคอร์ส
   - ระยะเวลา (เช่น 12 ชั่วโมง)
   - Badge (optional, เช่น "ยอดนิยม")
3. คลิก "บันทึก"

#### แก้ไขคอร์ส:
- คลิกปุ่ม "✏️" ที่คอร์สที่ต้องการแก้ไข

#### ลบคอร์ส:
- คลิกปุ่ม "🗑️" และยืนยันการลบ

### 4.4 จัดการแพ็กเกจราคา
ไปที่ **💰 จัดการแพ็กเกจ**

#### Tab แพ็กเกจ:
- เพิ่ม/แก้ไขแพ็กเกจ (ชื่อ, ราคา, ช่วงเวลา)
- ตั้งเป็น Featured (เน้น)
- เพิ่ม Badge

#### Tab Features:
- เพิ่ม/แก้ไข Features (เช่น "เข้าถึงคอร์ส", "ใบรับรอง")

#### Tab ตั้งค่า Features:
- กำหนดว่าแต่ละแพ็กเกจมี Features อะไรบ้าง
- ใส่ค่า (เช่น "10 คอร์ส", "✓", "✗")

### 4.5 ข้อมูลติดต่อ
ไปที่ **ตั้งค่าเว็บไซต์ > 📍 ติดต่อ & แผนที่**

| ฟิลด์ | คำอธิบาย |
|-------|----------|
| หัวข้อ | เช่น "ติดต่อเรา" |
| ที่อยู่ | ที่อยู่บริษัท |
| เบอร์โทร | หมายเลขโทรศัพท์ |
| อีเมล | อีเมลติดต่อ |
| LINE ID | @lineaccount |
| ละติจูด/ลองจิจูด | พิกัดแผนที่ |
| ระดับซูม | 1-19 (15 แนะนำ) |

### 4.6 Footer
ไปที่ **ตั้งค่าเว็บไซต์ > 📝 Footer**

| ฟิลด์ | คำอธิบาย |
|-------|----------|
| ข้อความ Footer | เช่น "© 2026 Company" |
| Facebook URL | ลิงก์ Facebook Page |
| Instagram URL | ลิงก์ Instagram |
| YouTube URL | ลิงก์ YouTube Channel |
| Twitter/X URL | ลิงก์ Twitter |
| LinkedIn URL | ลิงก์ LinkedIn |
| TikTok URL | ลิงก์ TikTok |

---

## 5. การจัดการธีม

### 5.1 เลือก Theme Preset
ไปที่ **🎨 จัดการธีม**

มี 5 ธีมให้เลือก:
| ธีม | สไตล์ |
|-----|-------|
| Dark Purple | ม่วงเข้ม (Default) |
| Dark Blue Ocean | น้ำเงินเข้ม |
| Dark Forest | เขียวเข้ม |
| Light Modern | สว่างโมเดิร์น |
| Light Warm | สว่างอบอุ่น |

### 5.2 ปรับแต่งสีเอง
ไปที่ **ตั้งค่าเว็บไซต์ > 🎨 สี**

| สี | การใช้งาน |
|----|----------|
| Primary | สีหลัก (ปุ่ม, ลิงก์) |
| Secondary | สี Gradient |
| Accent | สีเน้น |
| Background | สีพื้นหลัง |
| Card | สีการ์ด |
| Text | สีข้อความ |

### 5.3 เลือกฟอนต์
ไปที่ **ตั้งค่าเว็บไซต์ > 🔤 ฟอนต์**

ฟอนต์ไทยที่รองรับ:
- Noto Sans Thai
- Sarabun
- Prompt
- Kanit
- IBM Plex Sans Thai

---

## 6. การตั้งค่า SEO

### 6.1 Meta Tags
ไปที่ **🔍 ตั้งค่า SEO**

| ฟิลด์ | คำอธิบาย | แนะนำ |
|-------|----------|-------|
| SEO Title | หัวเรื่องในผลค้นหา | 50-60 ตัวอักษร |
| Meta Description | คำอธิบายในผลค้นหา | 150-160 ตัวอักษร |
| Keywords | คำค้นหา | คั่นด้วย , |
| Author | ผู้เขียน | ชื่อบริษัท |
| Robots | การ Index | index, follow |
| Canonical URL | URL หลัก | ถ้า duplicate |
| OG Image | รูปแชร์ Facebook | 1200x630 px |

### 6.2 Schema.org
ข้อมูลโครงสร้างสำหรับ Search Engine และ AI:

| ฟิลด์ | คำอธิบาย |
|-------|----------|
| Schema Type | EducationalOrganization / Organization |
| Organization Name | ชื่อบริษัท |
| Phone | เบอร์โทร (รูปแบบ +66-...) |
| Price Range | ช่วงราคา (฿, ฿฿, ฿฿฿) |
| Rating | คะแนน (1-5) |
| Review Count | จำนวนรีวิว |

### 6.3 Social Media Tags
ระบบสร้าง Open Graph และ Twitter Cards อัตโนมัติ:
- **og:title** - หัวข้อแชร์ Facebook
- **og:description** - คำอธิบายแชร์
- **og:image** - รูปภาพแชร์
- **twitter:card** - รูปแบบ Twitter Card

---

## 7. การตั้งค่าแผนที่

### 7.1 OpenStreetMap (ฟรี - แนะนำ)
ใน `config/config.php`:
```php
define('MAP_PROVIDER', 'openstreetmap');
```

ไม่ต้องใช้ API Key!

### 7.2 Google Maps
```php
define('MAP_PROVIDER', 'google');
define('GOOGLE_MAPS_API_KEY', 'AIza...');
```

ต้องสมัคร API Key จาก Google Cloud Console

### 7.3 ตั้งค่าพิกัด
ไปที่ **ตั้งค่าเว็บไซต์ > 📍 ติดต่อ & แผนที่**

วิธีหาพิกัด:
1. ไปที่ [Google Maps](https://maps.google.com)
2. คลิกขวาที่ตำแหน่ง
3. คลิก "What's here?"
4. คัดลอกตัวเลข (ละติจูด, ลองจิจูด)

---

## 8. การย้าย Domain

### ขั้นตอนการย้าย

1. **Backup ไฟล์และ Database**
   ```bash
   mysqldump -u root -p landingap_db > backup.sql
   ```

2. **อัพโหลดไฟล์ไปยัง Server ใหม่**

3. **สร้าง Database ใหม่และ Import**
   ```sql
   CREATE DATABASE landingap_db CHARACTER SET utf8mb4 COLLATE utf8mb4_unicode_ci;
   ```
   ```bash
   mysql -u root -p landingap_db < backup.sql
   ```

4. **แก้ไข Config**
   
   **config/database.php:**
   ```php
   define('DB_HOST', 'new_host');
   define('DB_USER', 'new_user');
   define('DB_PASS', 'new_password');
   ```
   
   **config/config.php:**
   ```php
   define('SITE_URL', 'https://newdomain.com');
   ```

5. **ตรวจสอบ Permissions**
   ```bash
   chmod 755 uploads/
   chmod 755 logs/
   ```

6. **ทดสอบเว็บไซต์**

---

## 9. ความปลอดภัย

### 9.1 ฟีเจอร์ความปลอดภัย

| ฟีเจอร์ | คำอธิบาย |
|---------|----------|
| CSRF Protection | ป้องกันการโจมตี Cross-Site Request Forgery |
| Rate Limiting | จำกัดจำนวน Login ผิดพลาด |
| Password Hashing | เข้ารหัสด้วย BCRYPT |
| Input Sanitization | กรอง XSS และ SQL Injection |
| Secure Sessions | HttpOnly, SameSite cookies |
| Security Headers | CSP, X-Frame-Options |

### 9.2 คำแนะนำความปลอดภัย

1. **เปลี่ยนรหัสผ่าน Admin ทันที**
2. **ใช้ HTTPS** 
3. **ตั้งค่า Permissions ที่เข้มงวด**
4. **Backup ข้อมูลเป็นประจำ**
5. **อัพเดท PHP และ MySQL**

### 9.3 Security Logs
ตรวจสอบ Log ได้ที่ `logs/security.log`

---

## 10. การแก้ปัญหา

### ปัญหาที่พบบ่อย

#### ❌ ภาษาไทยแสดงผิดพลาด
**สาเหตุ:** Database ไม่ได้ใช้ UTF-8

**แก้ไข:**
```sql
ALTER DATABASE landingap_db CHARACTER SET utf8mb4 COLLATE utf8mb4_unicode_ci;
```

#### ❌ แผนที่ไม่แสดง
**สาเหตุ:** CDN โหลดไม่ได้

**แก้ไข:** ระบบมี fallback อัตโนมัติ หรือตรวจสอบ Internet

#### ❌ CAPTCHA ไม่แสดงรูป
**สาเหตุ:** GD Library ไม่ได้ติดตั้ง

**แก้ไข (Ubuntu):**
```bash
sudo apt-get install php-gd
sudo service apache2 restart
```

#### ❌ อัพโหลดรูปไม่ได้
**สาเหตุ:** Folder permissions

**แก้ไข:**
```bash
chmod 755 uploads/
chown www-data:www-data uploads/
```

#### ❌ 500 Internal Server Error
**สาเหตุ:** PHP Error

**แก้ไข:**
1. ตรวจสอบ PHP Error Log
2. ตรวจสอบ Config files
3. ตรวจสอบ Database connection

---

## 📞 ช่วยเหลือเพิ่มเติม

หากมีปัญหาหรือคำถาม:
- ดู README.md
- ตรวจสอบ Security Log
- ติดต่อผู้พัฒนา

---

**Last Updated:** January 2026
**Version:** 1.0.0
